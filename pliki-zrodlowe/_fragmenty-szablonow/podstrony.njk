<ul class="flex flex-wrap justify-center -mx-4 mt-4">
{%- for podstrona in strona -%}
  <li class="w-full xs:w-1/2 sm:w-1/3 px-3 sm:px-4 mt-6 sm:mt-8">
    <a href="{{ podstrona.url }}" class="przycisk">{% include "ikony/" + podstrona.fileSlug + ".svg" %} {{ podstrona.data.title }}</a>
  </li>
{%- endfor -%}
</ul>